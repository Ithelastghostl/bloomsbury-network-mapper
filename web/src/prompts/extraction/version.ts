/**
 * Extraction prompt versioning metadata (PRD R1.4).
 *
 * Recorded in app.extraction_runs for every extraction so results
 * can be traced to the exact prompt, model, and temperature used.
 */
export const EXTRACTION_PROMPT_VERSION = 'v1.0.0';
export const EXTRACTION_MODEL = 'claude-sonnet-4-20250514';
export const EXTRACTION_TEMPERATURE = 0;
